{
  "name": "door-webhook",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.8",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.1"
  }
}
